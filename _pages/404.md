---
layout: page
permalink: /404.html
title: "Page not found"
description: "This page doesn't exist, or it has moved."
---

Try one of these:

<!-- Built from the same sources as the navbar (_includes/header.liquid), so new nav items appear here too. -->
<ul>
<li><a href="{{ '/' | relative_url }}">Home</a></li>
{%- assign nav_entries = site.pages | concat: site.data.nav_dropdowns -%}
{%- assign sorted_pages = nav_entries | sort: 'nav_order' -%}
{%- for p in sorted_pages -%}
{%- if p.nav and p.autogen == null -%}
{%- if p.dropdown %}
<li>{{ p.title }}:
{%- for child in p.children -%}
{%- unless child.title == 'divider' %}
<a href="{% if child.permalink contains '://' %}{{ child.permalink }}{% else %}{{ child.permalink | relative_url }}{% endif %}">{{ child.title }}</a>{% unless forloop.last %},{% endunless %}
{%- endunless -%}
{%- endfor -%}
</li>
{%- else %}
<li><a href="{% if p.permalink contains '://' %}{{ p.permalink }}{% else %}{{ p.url | relative_url }}{% endif %}">{{ p.title }}</a></li>
{%- endif -%}
{%- endif -%}
{%- endfor %}
</ul>

{% if site.search_enabled %}
You can also <a href="#" onclick="openSearchModal(); return false;">search the site</a>.
{% endif %}

<script>
  window.addEventListener('load', function() {
    if (window.goatcounter) {
      window.goatcounter.count({ path: '404: ' + location.pathname + location.search });
    }
  });
</script>
